part of verification_view;

class _VerificationTablet extends StatelessWidget {
  final VerificationController verificationController;
  const _VerificationTablet({Key? key, required this.verificationController}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
