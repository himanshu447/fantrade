part of splash_view;

class _SplashTablet extends StatelessWidget {
  final SplashController splashController;

  const _SplashTablet({Key? key, required this.splashController})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
