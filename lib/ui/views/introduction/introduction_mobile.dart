part of introduction_view;

class _IntroductionMobile extends StatelessWidget {
  const _IntroductionMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
