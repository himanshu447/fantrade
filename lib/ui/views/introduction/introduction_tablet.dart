part of introduction_view;

class _IntroductionTablet extends StatelessWidget {
  const _IntroductionTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
