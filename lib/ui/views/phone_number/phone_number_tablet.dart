part of phone_number_view;

class _PhoneNumberTablet extends StatelessWidget {
  final PhoneNumberController phoneNumberController;

  const _PhoneNumberTablet({Key? key, required this.phoneNumberController})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
