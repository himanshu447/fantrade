part of home_view;

class _HomeTablet extends StatelessWidget {

  final HomeController homeController;
  const _HomeTablet({Key? key,required this.homeController}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
