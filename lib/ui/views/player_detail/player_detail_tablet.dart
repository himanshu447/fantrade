part of player_detail_view;

class _PlayerDetailTablet extends StatelessWidget {

  final PlayerDetailController playerDetailController;

  const _PlayerDetailTablet({Key? key, required this.playerDetailController}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
